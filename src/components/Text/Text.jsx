import AlertDefault from "../Alert/AlertDefault";

const Text = ({name, age}) =>{
    console.log(props)
    return(
        <AlertDefault nameAlert={name} ageAlert={age}/>
    )
}
export default Text 